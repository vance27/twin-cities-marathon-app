# Database Configuration
POSTGRES_PASSWORD=tcm_password

# Application Configuration
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Database URL (used by the application and Prisma)
# For development: use localhost, for Docker: use service name "postgres"
DATABASE_URL=postgresql://tcm_user:tcm_password@localhost:5433/tcm_db

# Optional: Custom port mapping (uncomment and modify in docker-compose.yml if needed)
# TCM_APP_PORT=3000
# POSTGRES_PORT=5432