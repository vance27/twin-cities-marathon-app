# Database Configuration
POSTGRES_PASSWORD=tcm_password

# Application Configuration
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Database URL (used by the application)
# This is automatically constructed in docker-compose.yml, but can be overridden
DATABASE_URL=postgresql://tcm_user:tcm_password@postgres:5432/tcm_db

# Optional: Custom port mapping (uncomment and modify in docker-compose.yml if needed)
# TCM_APP_PORT=3000
# POSTGRES_PORT=5432